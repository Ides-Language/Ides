language: cpp
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq cmake flex bison llvm-3.0 llvm-3.0-dev libclang-dev libclang-common-dev libboost1.48-dev libboost1.48-all-dev

notifications:
  email: false
  irc: 
    channels:
      - "irc.freenode.org#ides"
    use_notice: true
    skip_join: true

compiler:
  - gcc
  - clang
# Change this to your needs
script: cmake . && make && make test && make install

