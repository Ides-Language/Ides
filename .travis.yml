language: cpp
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq cmake flex bison llvm-3.0-dev libclang-dev libboost1.48-dev

notifications:
  irc: 
    channels:
      - "irc.freenode.org#ides"
    use_notice: true
    skip_join: true

compiler:
  - gcc
  - clang
# Change this to your needs
script: cmake . && make

