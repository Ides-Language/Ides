language: cpp
before_install:
 - sudo apt-get update -qq
 - sudo apt-get install -qq cmake flex bison llvm-dev libclang-dev libboost-dev

compiler:
  - gcc
  - clang
# Change this to your needs
script: cmake . && make

